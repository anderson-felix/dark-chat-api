name: Training Actions

description: First actions

inputs:
  name:
    description: Insert your name
    required: true

  age:
    description: Insert your age
    required: true

runs:
  using: composite
  steps:
    - name: first step
      user: ./.github/actions/ssh
      with:
